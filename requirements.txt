--extra-index-url https://download.pytorch.org/whl/cpu

# Core framework
# starlette pinned below 1.4.0 to prevent GZipResponder.__init__ crash
streamlit==1.41.0
starlette<1.4.0

# Deep learning (CPU-only, fits Streamlit Cloud 1 GB RAM limit)
torch==2.6.0+cpu
torchvision==0.21.0+cpu

# Image & data
Pillow
numpy
matplotlib

# NOTE: grad-cam / pytorch-grad-cam is intentionally NOT listed here.
# pytorch_grad_cam does `import cv2` internally, and opencv-python crashes
# on Streamlit Cloud with: libgthread-2.0.so.0: No such file or directory
# GradCAM is implemented from scratch in app.py using pure PyTorch + PIL.
