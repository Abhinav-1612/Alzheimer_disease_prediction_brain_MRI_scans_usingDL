--extra-index-url https://download.pytorch.org/whl/cpu

# ── Core ─────────────────────────────────────────────────────────────────────
streamlit==1.41.0
# Pin starlette below 1.4.0 to avoid GZipResponder.__init__ crash (Issue #2)
starlette<1.4.0

# ── Deep Learning (CPU-only, safe for Streamlit Cloud 1 GB RAM) ──────────────
torch==2.6.0+cpu
torchvision==0.21.0+cpu

# ── Image / data ─────────────────────────────────────────────────────────────
Pillow
numpy
matplotlib

# ── GradCAM dependencies listed individually (grad-cam itself omitted) ───────
# grad-cam is NOT listed here because it declares opencv-python as a required
# dependency. When pip installs opencv-python on Streamlit Cloud it crashes with
#   ImportError: libgthread-2.0.so.0: cannot open shared object file
# Instead we list all of grad-cam's OTHER deps below, and the app installs
# grad-cam itself at startup with --no-deps (see app.py).
scikit-learn
tqdm
ttach
